<view class="bg">

    <!-- <view class='top-swiperView normalView'>

        <swiper id='swiper-ID' display-multiple-items="{{classArr.length>=6?6:classArr.length}}">

            <block wx:for="{{classArr}}" wx:key="index">

                <swiper-item data-id="{{item.id}}" class="{{swiperCurrentIndex==item.id?'select-swiper-css':''}}" bindtap='swiperBindtap'>
                    <text>{{item.title}}</text>
                    <text class='swiper-line' wx:if="{{swiperCurrentIndex==item.id}}"></text>
                </swiper-item>

            </block>

        </swiper>

    </view> -->

    <view class='viewLine'></view>

    <view class='scrollView'>
        <contentTxt id="content" userInfo="{{userInfo}}" hasUserInfo="{{hasUserInfo}}" contentArr="{{contentArr}}" bindmyevent="onMyevent" srcDomin="{{srcDomin}}" showBotTxt="{{showBotTxt}}"></contentTxt>
        <view class='loading' wx:if="{{ifloadtxt}}">
            加载中 . . .
        </view>
    </view>

</view>